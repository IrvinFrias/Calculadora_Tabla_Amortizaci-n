const Result = ({value}) => {
    return(
        <div>
            <h2 className="text-red mt-3">Total</h2>
            <span className="mt-2 fs-4 fw-semibold">$ {value}</span>

        </div>
    )
}
export default Result;